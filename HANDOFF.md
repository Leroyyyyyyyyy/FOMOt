# FOMOt 交接文档

复刻 Telegram 频道「FOMO NOW」那种代币监控推送：按节拍扫描 Robinhood Chain 上的活跃新币，
命中规则推一张告警卡片，+0.8s 采集持币初值，+5m1s 复核并**原地改写**原消息。

最后更新：2026-09-05。技术细节和链上事实见 [README.md](README.md)，这份只讲**状态、结构、问题**。

---

## 1. 当前状态

**能跑，链上和 FOMO 社交两半都通。但「已完全复刻」这个结论在 2026-09-05 的复核中被推翻了**——
详见 [docs/FIELDS.md](docs/FIELDS.md) 和本文 §8。这一节只讲现在**确实**成立的事。

> ⚠️ 本文 2026-09-05 之前的版本写过「全通」「全部修复」「账户映射已验证」「结构、语义、量级全部对得上」。
> 这些结论**不成立**，已在 §8 逐条纠正。看历史结论前先看 §8。

当前通知模式默认 **`off`（禁发送）**：不做任何 Telegram 网络调用，
初始发送、复核改写、复核撤回三条路径全部只进本地记录器（`notification_log` 表 + `data/fomot.notifications.jsonl`）。
要真发消息必须显式 `NOTIFY_MODE=telegram` 且凭据齐全；`src/notify/telegram.ts` 里还有一道兜底闸门。

### 卡片现在长这样（禁发送演练，2026-09-06）

复核那一刻（PnL 还没就绪）：

```
持币初值（+0.8s）: 全链 178 · Fomo 72 (40.4%)
持币复核（+5m1s）: 全链 491 · Fomo 336 (68.4%)

Fomo 24H盈利榜持有人: 已确认至少 2 人
• #28 frank · 11.02M MEME · 粉丝 218.71K · 全平台24h +$323.15K
Top10持币账户（+5m1s）· 账户识别 10/10
  该币累计收益（已实现+未实现）: +$1.88M · 盈利 7 人
  全平台24H PnL: 采集中…（0/10）

健康: 🟢 实时源正常 · 持币覆盖 49/336 · Top10 10/10 · 浏览器→入库 64ms · 禁发送演练
```

PnL 就绪后**原地改写同一条卡片**，持币两行与两个偏移一个字都不变：

```
持币初值（+0.8s）: 全链 178 · Fomo 72 (40.4%)
持币复核（+5m1s）: 全链 491 · Fomo 336 (68.4%)
...
Top10持币账户（+5m1s）· 账户识别 10/10
  该币累计收益（已实现+未实现）: +$1.88M · 盈利 7 人
  全平台24H PnL: +$607.15K · 盈利 7 人 · 整点对齐口径（截至 09/06 12:00）
```

数据源时间差超限时会多一行（如实说明，不当成同一时点）：

```
⚠️ 数据时点: 链上与 Fomo 采集相差 300s
```

跟原版截图相比有四处**故意的**差异，都是为了不说假话：

1. 原版那一行 `Top10持币账户 全平台24H PnL` 在本项目里拆成了两行。原因见 §8.1——
   旧代码把「该币累计收益」求和后标成「全平台24H PnL」，那是个假标签。
2. 全平台 24H PnL 有**自己的盈利人数**，与该币那行各算各的；缺失时是 `n/a（N/M）· 原因`，
   不用该币收益或该币盈利人数顶替。默认开启，`FOMO_PLATFORM_PNL=0` 可关。
3. 全平台那行会标出口径与**窗口截止时间**（整点对齐），因为「刚抓到」不等于「数据最新」。
4. 盈利榜交集在身份覆盖不全时写「已确认至少 N 人」，因为没发现 ≠ 不存在。

> 截图里的 `+$500.31K／7 人` 只当**渲染样例**，从未被写成期望值。计算侧的验收用例
> 全部带逐人输入，见 `scripts/trace-pnl.ts` 与 `tests/pnl.test.ts`。

### 关键指标实测

| 指标 | 数值 | 备注 |
|---|---|---|
| 初值快照延迟 | **+1.4s ~ +4.4s** | 修复前是 +54.6s，见 §5.1 |
| 复核延迟 | **+2m1s ~ +2m9s**（配置 120s） | 睡到绝对时间点，不会漂 |
| 扫链 tick | 中位 **约 1.2s**，但有长尾（40s / 90s / 165s 尖峰） | 落后区块始终 0，能追回来；见 §6「扫链有长尾」 |
| 浏览器→入库 | 1~61ms | 原版 64ms |
| 持币人数准确度 | 与 Blockscout 偏差 **-0.6% ~ +3.4%** | AGI 3031 vs 3050；Inkky 84 vs 85 |
| RPC 降级频率 | 约 **17.5 次/分**（约 39 分钟 682 次） | 与修前（18.7 次/分）基本持平，见 §5.8 |

### 代码量

约 3150 行 TypeScript，`npx tsc --noEmit` 通过。

---

## 2. 架构

### 数据流

```
                    ┌─ Robinhood Chain RPC ──┐
                    │  主 + 备用，按序降级      │
                    │  PoolManager 日志        │
                    │   Initialize → 新池      │
                    │   Swap       → 成交量    │
                    │  代币 Transfer 日志 → 持币人
                    └───────────┬──────────────┘
                                ↓
   Blockscout ──→ 部署区块（持久化到 SQLite，只查一次）
                                ↓
                        ┌───────────────┐
   fomo.family ──→      │    SQLite     │
   (登录态浏览器)   ──→  │  pools/swaps  │
   /v2/leaderboard/24h  │  holders/fomo │
   /hodlers/top         └───────┬───────┘
                                ↓
                    每秒一个 tick：候选集 → 量价门 → 币龄门
                                ↓
                    触发（同一 tick 共用时间戳）
                                ↓
              +0.8s 持币初值 → 渲染 → Telegram 发送
                                ↓
              +5m1s 复核 → FOMO 门 → 改写消息 / 撤回
```

### 模块

| 目录 | 模块 | 职责 |
|---|---|---|
| `chain/` | `client` | viem 客户端、区块时钟（100ms 出块下用插值给日志打时间戳）、`withRetry` |
| | `abi` `quotes` `pricing` | V4 事件签名、计价资产白名单、sqrtPriceX96 → 美元价 |
| | `watcher` | 主扫描循环：拉 PoolManager 日志 → 建池入库 / 成交量入库 |
| | `logrange` | 自适应日志范围（RPC 限单次命中 ≤10000 条） |
| | `holders` | 放代币 Transfer 日志重建余额表 → 全链持币人数 + Top10 |
| | `deployment` | 代币部署区块（**持久化**，触发路径只读缓存） |
| | `blockscout` `backfill` `volume` `marketdata` | 浏览器 API、启动回补建池、滚动窗口成交量、行情快照 |
| `fomo/` | `session` `login` `auth` | Playwright 持久 profile、手动登录、登录态探测 |
| | `scraper` | 拦截 API 响应 → 解析 → 落库；导航串行 + 优先级 |
| | `api` | `/v2/leaderboard/24h` 和 `/hodlers/top` 的精确解析 |
| | `extract` | 形状启发式提取（只当兜底，从其他端点顺手捞身份） |
| | `provider` | 抽象接口，FOMO 不可用时降级为纯链上 |
| `engine/` | `index` | 触发判定、两段门、快照调度、复核改写 |
| `notify/` | `render` `telegram` | 卡片渲染、发送/改写/撤回（带限速队列） |

### 两个设计要点

**同一 tick 共用「首次触发」时间戳。** 原版截图里两个不同的币时间戳精确到毫秒完全相同
（`04:56:44.727`），说明它是按节拍扫候选集，不是逐事件流式触发。这里照抄了这个模型。

**触发路径上不发任何网络请求。** 部署区块和持币余额表必须**预热完成**才允许触发，
没备好就交给后台慢慢弄、这一轮跳过。原因见 §5.1。

---

## 3. 数据分两半

| | 来源 | 状态 |
|---|---|---|
| 市值 / 成交量 / 全链持币人 | 链上，公开免费 | 通。持币人数与 Blockscout 偏差 -0.6%~+3.4% |
| Top10 持币账户 | FOMO `/hodlers/top` 前 10 | 通，但**集合定义是暂定口径**，见 docs/FIELDS.md §4 |
| Fomo 用户占比 / 粉丝数 / 24H 盈利榜 | FOMO App | ✅ 通，但**需要你自己的账号** |

`prod-api.fomo.family` 未鉴权一律返回 430。社交那半只能用登录态浏览器抓。
原版卡片最后一行的 `浏览器→入库 64ms` 就是这个管线的延迟埋点——他们也是这么干的。

两个端点撑起整个社交部分：

| 端点 | 内容 | 对应卡片 |
|---|---|---|
| `/v2/leaderboard/24h` | 150 人榜单（名次靠数组下标，**接口无 rank 字段**） | `#28 frank`、粉丝数、`全平台24h` |
| `/hodlers/top` | 某币的 FOMO 持有人总数 + Top50 | `Fomo 336`、Top10 那一段 |

`/hodlers/top` 的 `totalHolders` **直接就是「持有该币的 FOMO 用户数」**，
不用自己维护钱包↔身份映射表。

### 3.1 备用 RPC

双端点，**严格按顺序降级，不按延迟排序**——低延迟节点不一定数据全。
候选逐个实测筛出来（`npm run probe-rpcs`），注册表列的 7 个里只有 1 个合格：

| 端点 | 结果 |
|---|---|
| `rpc.mainnet.chain.robinhood.com` | ✅ 主 |
| `rpc-robinhood.blockmachine.io` | ✅ 备。与主节点逐条一致（同范围 Init 34 / Swap 4327），历史交易可读，**且无 10000 条日志上限** |
| `robinhood-rpc.publicnode.com` | ❌ 只服务最近约 20 块，"Archive requests require a personal token" |
| `robinhood.api.pocket.network` | ❌ 同上，"historical state is not available" |
| `arrowrpc` / `ordofi` / `nodeflare` | ❌ 返回 HTML，不是 JSON-RPC |
| `lb.routeme.sh` | ❌ 公共额度已耗尽 |

**筛选标准不是「能连上」，而是「返回的数据和主节点逐条一致」**——
静默返回不完整日志的节点会让持币人数和成交量直接算错，比没有备用更危险。

用 `RPC_URLS`（逗号分隔）覆盖顺序。降级打 WARN 日志，当前端点记在健康指标 `rpc_endpoint`。

---

## 4. 怎么跑

```bash
npm install
cp .env.example .env      # 填 TELEGRAM_BOT_TOKEN / TELEGRAM_CHAT_ID
npm run login             # 一次性：在它打开的那个窗口里登录 fomo.family
npm start
```

⚠️ **`npm run login` 开的是独立浏览器、独立 profile，你日常 Chrome 里的 FOMO 登录不会带过来**，
必须在新窗口里重新登录。Chrome 136+ 禁止对默认 profile 开远程调试端口，所以也挂不上你现有的 Chrome。

不填 Telegram 也能跑，卡片打到终端。

### 排查命令

| 命令 | 用途 |
|---|---|
| `npm run check-session` | FOMO 会话还有效吗（会明确说是没登录还是失效） |
| `npm run diagnose` | 导航轨迹 / 请求域名 / 每个接口能提取多少条 / 控制台报错 |
| `npm run explain` | **逐个候选打印被哪一道门卡住**——调阈值时最有用 |
| `npm run check-rules` | 规则文件是否全部解析成功 |
| `npm run backtest` | 拿原版截图的两条样本回测当前阈值 |
| `npm run status` | 库里的池 / 候选 / 健康指标 |
| `npm run dry-run` | 阈值放宽 + 2 分钟复核，验证整条链路 |

`scripts/probe-*.ts` 是当初逐项核实链上事实用的探针，留着方便复查。

---

## 5. 踩过的坑（对后续最有价值的部分）

### 5.1 `deployment()` 曾把「+0.8s 快照」拖到 +54.6s

卡片上写着 `+0.8s`，实测却是 `+54.6s`。**第一次猜是 `AdaptiveRange` 每次重新试探日志密度，量完发现完全不是。**
真正原因是 `deployment()` 在关键路径上，而它依赖的两个 Blockscout 端点：

```
/api/v2/stats                      HTTP 200   0.5s   ← 服务正常，没封我们
/api?module=contract&...creation   HTTP 429   0.4s   ← v1 被限流
/api/v2/addresses/{ca}             HTTP 000  49.6s   ← 这个端点直接挂死
```

四处修：部署区块持久化到 SQLite（永不变的事实，只付一次代价）、`deployment()` 加 9 秒总体超时、
单次请求超时 12s → 6s 并识别 429、**触发路径改成只读缓存（`deploymentCached`）**。

修完 +1.4s ~ +2.2s。

**教训：卡片上承诺的延迟指标，其路径上不能有任何第三方依赖。**

### 5.2 FOMO 门放错了阶段

FOMO 自己的持币索引对新币有延迟。拿本项目记录的 `holder_snapshots` 回测生产规则：

```
0x2240d02f  初值 33/188 = 17.6% → 被拒
            但它两分钟后的复核是 100/231 = 43.3%，轻松通过
0x96639854  初值 68/282 = 24.1% → 被 0.9 个百分点拒掉
```

七次记录的触发里**四次是被初值阶段的 FOMO 比例误杀的**。
现在由 `rules.yaml` 的 `fomo_gate_stage` 控制，默认 `recheck`，不过关则撤回已发出的消息。

### 5.3 `eth_getLogs` 的限制是「命中日志 ≤10000 条」，不是区块范围

viem 会把它显示成 `Missing or invalid parameters`，极具误导性。
真实报文是 `{code:-32000, message:"logs matched by query exceeds limit of 10000"}`。
Swap 约 5.2 条/块（800 块 = 8407 条，1600 块超限），Initialize 约 0.024 条/块——
密度差 200 倍，两个事件必须各自维护自适应范围。

### 5.4 headless 的 UA 里带 `HeadlessChrome`，API 直接拒

拒绝响应**不带 CORS 头**，所以浏览器控制台报的是
`blocked by CORS policy: No 'Access-Control-Allow-Origin' header`——看着像跨域配置问题，实际是反爬。
覆盖成正常 Chrome UA 后：prod-api 请求从 6 次全败变成 40 次全成。

### 5.5 公共 RPC 不是归档节点，别用 `eth_getCode` 二分查部署区块

往回 1000 块能查，10 万块就报错。二分会把「查询失败」当成「尚未部署」，
静默返回偏晚的区块——实测某个币因此少算了 **87%** 的持币人。
**静默给错答案比直接失败更糟。**

### 5.6 代币部署区块 ≠ 建池区块

实测 AGI 的池子比代币部署晚约 100 分钟。按建池区块扫 Transfer 会算成 127 人，实际 639。

### 5.7 `typecheck` 通过 ≠ 改动生效

这一轮有三处 `str.replace` 补丁**静默没匹配上**（清理旧行的调用、按代币唤醒的 notify），
而删除性的改动本来就不会引入类型错误，所以编译通过完全说明不了问题。
后果：榜单陈旧行照旧堆积（281 行占 150 个名次），`tokenStats` 每次空等 20 秒超时。

**改完必须 grep 核实落地，再用运行时数据验证。**

同理，**量性能要跑满几分钟**：`shouldThrow` 那次我先拿一个 43 秒的启动期样本
得出「降级少了 90%」，跑满 10 分钟后真实数字是 21%（见 §5.8）。启动期不代表稳态。

### 5.8 备用 RPC 差点掩盖了我们自己的 bug

加完备用端点后，降级异常频繁（3 分钟 56 次），扫链 tick 从 0.7s 涨到 **10s**。
**第一次判断是「单端点重试 2 次太贵」，改成 0 后毫无改善——判断错了。**
实测当时两个端点都 5/5 成功、延迟都在 1.5s 内，根本不是端点不健康。

真正原因：我们的查询打到了**主端点的 10000 条日志上限**，而备用端点没有这个限制，
把这类查询照单全收。于是 `AdaptiveRange` 永远看不到限流错误、范围只涨不缩，
每个请求都必然先在主端点失败一次再降级。

**备用端点在掩盖我们自己的 bug——这比慢更值得警惕。**

修法：用 viem fallback 的 `shouldThrow`。「单次命中日志过多」是**我们查询太大**的语义错误，
不是端点故障，不该触发降级；让它直接抛给自适应逻辑去缩范围。
**这个修复的真实效果，要说准确**（跑满约 39 分钟的完整数据）：

| | 修前 | 修后 |
|---|---|---|
| 扫链 tick 中位 | 10153ms | **约 1.2s** ✅ |
| 警告 / 错误 | 3 / 3 | **1 / 0** ✅ |
| 降级频率 | 18.7 次/分 | 17.5 次/分 —— **基本没变** |

**它修的是 tick 耗时和错误，不是降级频率。** 降级是主端点真实的抖动，
本来就会发生，备用端点接住它们正是它该干的事。

这个数字我改口过两次，值得记：先拿 43 秒启动期样本得出「降了 90%」，
再拿 10 分钟样本得出「降了 21%」，跑满 39 分钟才是「基本持平」。
**量性能必须跑满足够长的窗口，启动期和短窗口都会骗人。**

顺带修了 `isLogLimitError` 的假阳性——它原来匹配错误码 `-32000`，
而重试标签里带着区块范围（`getLogs Swap 31401-32000 重试…`），
一个以 32000 开头的区块号会让普通网络错误被误判成超限。现在只匹配报文文本。

---

## 6. 已知问题 / 未做的事

### 公共 RPC 会抖（已缓解）

官方公共 RPC 文档明确写着「rate limited、无归档、无 SLA、不适合生产流量」，实测确实会抖。
现已配置双端点按序降级（见 §3.1 和 §5.8），最近一轮 0 警告 0 错误。

仍需注意：降级不是万能的，主端点长时间劣化时延迟会上升。健康指标 `rpc_endpoint`
记着当前在用哪个端点，降级会打 WARN 日志。

### 2026-09-05 P1 / P2 review 已处理

- 告警复核状态、到期时间和错误写入 SQLite；进程重启会恢复 `pending_recheck`。实测重启前
  4 条待复核，重启后全部被接管，恢复卡片能输出 Top10 PnL。
- ~~复核刷新严格执行新鲜度约束……刷新失败保留任务、60 秒后重试。~~
  **不成立**：`skip_when_unavailable=true` 时空 enrichment 会被规则放行，任务随即标 `completed`，
  根本不会重试。已在 2026-09-05 修复（三态门），见 §8.3。
- ETH/USD 增加 Coinbase 与 CoinGecko 交叉回退；报价失败时整批 Swap 重试，游标不前移。
- ~~holder 缓存每次回退并重放最近 64 块，拒绝 head 倒退。~~
  **不成立**：回退是直接在**共享**余额表上做的，且发生在网络请求**之前**——请求一失败缓存就已经坏了，
  重试还会把同一批 delta 再撤一次。「拒绝 head 倒退」是抛异常，不是处理。已重写，见 §8.4。
- 冷启动回放完整 1 小时 Swap；窗口未完整、池回补未完成时暂停触发。
- 市场价选择近 5 分钟成交额最大的真实交易池，Initialize 空池不能覆盖主价格。
- 候选热路径只读内存与 SQLite；部署、持币表、FOMO 页面均未预热时不触发。
- 浏览器导航有 `复核 > 告警 > 预热 > 榜单` 优先级和截止时间；鉴权只认成功 API 响应并自动恢复。
- Top10 直接使用 `/hodlers/top` 的前 10 个账户及正负 PnL，不再依赖正收益榜。
- ~~按持仓金额唯一匹配并持久化……实测 CAP 一次学习到 32 个账户映射。~~
  **不成立**：金额匹配只能产生**候选**，证明不了钱包归属；「学到多少条映射」也不是正确性指标。
  437 条历史推断已全部隔离为未验证候选，见 §8.2 与 docs/MIGRATION.md。
- 健康行现在分别检查链游标、报价、FOMO 数据和榜单新鲜度；覆盖率使用已识别身份/FOMO 总人数。
- SQLite 启用 5 秒 busy timeout，Playwright async 回调有异常兜底；外部诊断争锁不再杀进程。
- RPC 关闭大型 `getLogs` HTTP 合批，响应过大/新版 `InvalidParams` 都会触发自适应缩片；
  持币调度器保留实时初值槽位，恢复复核与预热在后台串行，避免重启时压垮公共 RPC。
- 配置启动时 fail-fast；`backfillPools()` 失败转后台重试；生产判定和回测共用同一组纯函数。
- ~~`npm test`（7 项）通过。~~ 那 7 项里有几条把未经验证的收益口径写死成了正确答案。现为 92 项，见 §8.5。

### 一个语义选择，没改

`全链持币人` 把 PoolManager、`0xdead`、代币合约本身算进去了（`total++` 在过滤之前）。
**这不算 bug**：Blockscout 自己也这么算，之前 0.6%~3% 的对账一致性正是建立在口径相同上。
改了反而会和唯一的外部校验源分叉。要改的话记得同时改对账基准。

### 扫链有长尾（未解决）

24 次心跳的扫链耗时（毫秒，按时间顺序）：

```
895 40340 90740 2776 2127 2276 1157 1157 1059 1086 1214 634 1119 1119
3159 1293 662 165292 3998 609 4531 1764 874 3564
```

中位约 1.2s，但有 **40s / 90s / 165s 三个尖峰**。165s 那次之前紧跟着一串连续降级，
说明是主端点持续劣化时、每个请求都要先失败再降级累积出来的。

**影响有限但真实**：`落后区块` 始终是 0，卡顿后能追回来；但卡顿的那两三分钟里
不会有新池和新成交入库，候选发现被推迟。

没解决。可能的方向：给 fallback 加熔断（主端点连续失败 N 次后，暂时跳过它一段时间，
别每个请求都付一次失败往返）。viem 的 fallback 不带熔断，要自己包一层。

### FOMO 那三条已放松到与原版一致（2026-09-05）

`fomo_holders` / `fomo_holder_ratio` / `fomo_leaderboard_holders` 现在都是 `0`，
即**不拿 FOMO 数据当门，只在卡片上展示**。

依据是原版截图的两条样本，关键证据是 HDR：

```
MEME  Fomo 72 (40.4%) · 榜单 2 人  → 原版推了
HDR   Fomo 13  (7.8%) · 榜单 0 人  → 原版也推了
```

HDR 三项都很低（榜单甚至是 0）却照推不误，说明原版这三条的阈值必然
≤13 / ≤7.8% / =0，而榜单那条**根本不是门**。两个样本只能给出「原版不严于此」的
下界，给不出真实阈值，所以取 0——任何非零值都可能比原版更严。

`npm run backtest` 拿这两条样本回测当前规则，改完阈值可以随时验证。
想收紧的话，「仍能放过两条样本」的最紧设置是 `13 / 0.078 / 0`。

**副作用**：三条都是 0 之后，`fomo_gate_stage: recheck` 的复核门永远不会拒绝，
撤回路径实际上不会被触发。机制还在，收紧任意一条就会重新生效。

### 阈值需要按手感调

`config/rules.yaml` 里那组默认值是我按原版截图两条样本反推的
（MEME: MC $126K / 5m $22.36K / 1h $212K / 178 持有人；HDR: MC $91K / 5m $71K）。
实测在当时行情下能选出 3 个真正在爆量的币，不至于一天憋不出一条或满屏噪音——
但这组数**大概率需要按你自己的手感调**。用 `npm run explain` 看每个候选卡在哪道门。

### 没做的

- 目前有 7 项 enrichment/Top10/账户映射/日志缩片回归测试；链上部分仍主要靠主网 dry-run 与 Blockscout 对账。
- 没有进程守护 / 自动重启。
- Telegram 只发到一个频道，没有分级或多路由。
- `fomo_gate_stage: recheck` 的撤回逻辑只在终端模式下打了日志，
  真实 Telegram 撤回路径**没有实测过**（需要配置 bot token 才能跑到）。

---

## 7. 下一步建议

按性价比排序：

1. **实测 Telegram 全链路**（发送 / 改写 / 撤回）——这三条目前只在终端模式验证过，
   `fomo_gate_stage: recheck` 的撤回路径需要真实 bot token 才跑得到。
2. **按自己的手感调阈值**，用 `npm run explain` 迭代。
3. 三档剩下的：`backfillPools()` 失败会 `process.exit(1)`（应 best-effort）、
   几处死代码、`new URL(...).pathname` 不解码。`isLogLimitError` 那条已在 §5.8 修掉。
4. 长期跑的话加个进程守护 / 自动重启。

---

## 8. 2026-09-05 复核：被推翻的结论与整改

这一轮的出发点是：**截图证明不了原程序的阈值、Top10 集合定义或内部架构**。
所以把事实、假设、待验证分开重记了一遍。完整字段口径见 [docs/FIELDS.md](docs/FIELDS.md)。

### 8.1 「全平台24H PnL」是个假标签（已修）

旧代码：`top10Pnl = Σ /hodlers/top[i].pnl`，卡片上标成「全平台24H PnL」。

**证据表明这两个量根本不是一回事。** 同一用户、两侧采集只差 9~14 秒：

| 用户 | 代币 | `/hodlers/top.pnl` | 榜单 `pnl24h` | 采集时间差 |
|---|---|---|---|---|
| Binkieee | 0x2f21… | **−6,158.67** | **+552,794.61** | −9,161 ms |
| looc | 0x2f21… | −5,147.92 | +197,973.31 | −9,161 ms |
| ogle | 0x7dbf… | **−723.50** | **+658,581.21** | −13,501 ms |

十几秒的采集间隔解释不了这种量级和符号的差异。

同一用户在不同代币上的 `pnl` 也各不相同（Unipcs 五个代币：+7,672,640 / −21,274 / −3,020 / −30,171 / −0.05），
所以它是 **per (user, token)**，不可能是用户级别的平台总收益。
实测 50/50 个持有人满足 `pnl == realizedPnl + unrealizedPnl`，是**该币全时段累计**收益。


**影响面**（基线备份 `fomot-20260905-190501.db`，55 个代币的 `/hodlers/top` 快照）：

| 项 | 值 |
|---|---|
| 有 Top10 快照的代币 | 55 |
| 旧代码会**印出数字**的（十人齐全且收益齐全）| **51** |
| 其中印出**负数**的 | 25 |
| 数字范围 | **−$431,865.17 ~ +$29,573,679.70** |

也就是说这不是个别情况：51 张卡片会把「该币全时段累计收益」标成「全平台24H PnL」。
量级最大的 PONS 会印出 `+$29.57M`——而该币当时市值远不足以支撑这个数，
本身就是这个标签不成立的旁证。

| 代币 | 旧卡片会印的「全平台24H PnL」 |
|---|---|
| PONS | +$29,573,679.70 |
| SHROOM | +$4,156,891.31 |
| MEME | +$1,881,924.34 |
| SLINK | **−$431,865.17** |

**顺带把真正的口径也挖出来了**：

```
榜单 pnl24h = totalPnL(实时全时段累计) − aggregatedSnapshotById(userId, 最新整点−24h).pnl
```

单次同时刻复算误差 **1.4e-7**。而且 `/v2/userTokens/aggregatedSnapshot?userId=…` 对**任意**用户
（含未上榜、负收益）返回逐小时累计序列，所以普通用户的全平台 24H 收益是可算的——
已验证 Fartman Sacks −98,090、BNBULL −4,207 等负值样本。

**现在的做法**：类型上分成 `top10TokenPnl` 与 `top10PlatformPnl24h` 两个字段，卡片分两行两个标签。
后者默认 `n/a`（取数要十次浏览器导航，扛不住时间预算），`FOMO_PLATFORM_PNL=1` 可开，
且**十人全部取到且窗口一致**才给合计，否则 `n/a` + 覆盖率。

### 8.2 金额匹配不是身份证明（已隔离）

437 条 `fomo_trade_wallets` 全部来自「两位小数持仓唯一相同」。两位小数会碰撞、
两侧快照有采样错位、一个用户可能有多个钱包，EIP-7702 委托代码也不证明归属。

已迁移到带证据链的 `fomo_wallet_links`（`status` / `evidence_type` / `evidence_src` /
`chain_id` / `observed_ts` / `verified_ts` / `tx_hash` / `log_index`），**全部 437 条是 `candidate`，
`confirmed` 为 0**。聚合统计只读 `confirmed`，所以这些候选一条都不进榜单交集或覆盖率。
金额碰撞、快照错位、冲突映射（同地址两个 userId）都会被拒绝或隔离。详见 docs/MIGRATION.md。

### 8.3 「刷新失败一定重试」不成立（已修）

`fomoRejection` 把「数据没采到」和「规则通过」都返回 `null`。于是复核时 FOMO 刷新失败拿回空 enrichment，
在 `skip_when_unavailable=true` 下规则**放行**，任务被标成 `completed`——缺数据被记成了成功。

改成三态门 `pass / reject / missing`：
- `reject` → 撤回；
- `missing` → 重试，**不得**标完成，**也不得**撤回（不知道该不该撤的时候撤，就是误撤）；
- 超过 `original_due_ts + 15min` 仍不完整 → 终结为 `abandoned` / `collection_state='degraded'`，不无限重试。

`status`（是否通过筛选）与 `collection_state`（采集是否完成）现在是两列。
`original_due_ts` 与 `recheck_due_ts` 也分开，重试不再覆盖原定到期时间。

### 8.4 持币缓存的失败路径（已重写）

旧代码先在**共享** `balances` Map 上撤销最近 64 块，再去发网络请求；请求失败缓存已经坏了，
重试还会重复撤销同一批 delta。返回的快照又共享同一张可变 Map。

现在：回滚与重放全在临时副本上做，整轮成功才**原子替换**；同一 CA 在 holder 层串行化
（不再依赖 Engine 的全局并发上限）；返回的快照持有独立副本；用**区块哈希**验证连续性，
超出回放窗口就明确 `rebuilt`，不假装支持任意重组。

### 8.5 测试

7 项 → **92 项**，全部使用可注入的 RPC / provider / 时钟 / 通知 mock 和**独立临时库**
（`FOMOT_DB`，实测跑完全套后 `data/fomot.db` mtime 不变），不依赖实时主网。

旧 `enrich.test.ts` 把「Top10 PnL 求和 = 全平台24H」写死成了正确答案，已按证据重写。
`logrange.test.ts` 原来只测字符串识别，现在有真实分片失败 → 缩区间 → 完整取回且不重不漏的用例。

覆盖：收益（负/零/缺失/未上榜/窗口不一致）、Top10（重复用户/不足十人/身份全但收益缺/真实零人）、
映射（金额碰撞/旧记录未验证/多钱包/跨链/冲突）、持币缓存（回滚后失败再重试等于全量重建、
同 CA 并发、同高度换分支、head 倒退、旧快照不变）、复核（刷新失败且规则允许缺失仍不能误标成功）、
恢复（正常/强制退出、重复恢复、损坏 payload、过期 firing、发送成功但状态未落库的崩溃窗口）、
SQLite（批次中途失败整体回滚）、扫链（报价失败不越过未处理事件、重放不重复累计）、
调度（初值优先、并发上限、取消、后台不饥饿）。

### 8.6 其他已修

- **入库事务化**：总人数、Top 列表、旧行清理在同一事务里提交，「新鲜时间」最后写。
  以前中途失败会留下「时间是新的、榜是旧的」。「浏览器→入库」现在量到 COMMIT 结束。
- **时间戳分开**：链上区块与区块时间、FOMO 响应时间（`resp_ts`）、入库提交时间（`updated_ts`）、
  榜单时间、聚合完成时间各存各的，不再取最小值统一叫 `fomoTakenTs`。
- **在线判据**：以前任何 fomo 域名的 200（`/config`、`/static/*.js`）都会清零鉴权失败计数并把
  `fomo_source` 置成 ok——无关公共接口的 200 抵消了鉴权故障。现在只认 `/v2/leaderboard/24h`
  和 `/hodlers/top` 成功解析，并区分 `auth_error` / `parse_error` / `ingest_error` / 数据为空。
- **回退口径**：删掉 `top10Count || chainTopCount` 这类回退——真实的零不能被另一个集合的人数替换。
- **去重主键**：榜单交集按稳定 `userId` 去重，显示名不再当身份主键。

### 8.7 实跑验收（2026-09-05，禁发送模式，共 105 分钟）

主验收 60 分钟（生产规则）+ 修复验证 45 分钟。完整报告见 [docs/run/ACCEPTANCE.md](docs/run/ACCEPTANCE.md)。

- 12 条告警完成初值 + 复核，全部 `completed`/`collection_state='complete'`，复核重试 0 次；
- `ERROR` 0 条、未捕获异常 0 条；**Telegram 操作 0 次**（`notification_log` 全库：`off` 36 条、`telegram` 0 条）；
- 初值延迟 P50 **1.69s** / P95 6.06s——**0.8 秒目标未达成，如实申报**，不靠改标签掩盖；
- 实跑抓到一个真缺陷：共享浏览器页被关闭后无人重建，榜单刷新与预热全线失败、
  **触发在第 33 分钟后完全停摆**，而 `ready` 仍为 true 所以不自愈。已修复并单独跑 45 分钟验证
  （榜单刷新 9 次 / 0 失败，越过旧代码 +36.5 分钟的翻车点）。
  值得记的是**卡片当时没有说假话**：榜单超龄后显示 `n/a`、健康行转红——第四阶段要的降级是成立的。
- 仍未解决：一条复核晚 288 秒（P50 仅 5.3s，属长尾；疑似 prewarm 占住唯一后台槽，**未证实**）；
  RSS 45 分钟涨到 906MB；主端点 RPC 降级 363 次/小时（既有行为，未恶化）。

### 8.7b 全平台 24H PnL 已默认开启（2026-09-05 22:37~23:37 实跑 60 分钟）

> ⚠️ **这一节是历史记录，其中「只在复核阶段取」「复核变成约 +5m56s」的做法已被 §8.9 取代。**
> 现在 PnL 完全不在持币关键路径上，复核回到 `+5m1s` 量级。看现状请直接跳到 §8.9。

改成默认开启前踩了一个坑：它原本在 `enrich()` 里无条件调用，而 `enrich()` 在**初值**路径上，
实测把初值从 1.7s 拖到 **1m11s**（70,966ms）。改成只在 `priority === 'recheck'` 时取之后，
初值回到 **3,210ms**。这跟原版一致——原图那行标的就是「（+5m1s）」，是复核阶段的产物。

默认开启后的 60 分钟：37 次触发、15 条完整生命周期、**0 错误 / 0 未捕获 / 0 复核重试**、
榜单刷新 11 次 0 失败、Telegram 操作 0 次。
15 张复核卡里 11 张拿到真实数值（含 3 个负数），4 张 `n/a` 成因清楚（3 张是 Top10 空集，
1 张是十人只取到九人——差一个就不给合计）。

**代价**：复核延迟 P50 从 3.9s 变成 **56.6s**（卡片从 `+5m1s` 变成约 `+5m56s`），初值不受影响。
`FOMO_PLATFORM_PNL=0` 可关。详见 docs/run/ACCEPTANCE.md §9。

### 8.9 2026-09-06 整改：窗口校验、独立盈利人数、时间语义、PnL 解耦

四件事，按「先定数据口径与时间模型，再动调度」的顺序做的。

**① 全平台 24H 收益的严格窗口校验**（新增 `src/engine/pnl.ts`）

以前每条收益只有 `{value, window, asOfTs}`，`asOfTs` 既当窗口右端又当「刚抓到」；
取数时各人挑各自序列的最新点，起点找不到就退而求其次拿更早的点；缓存只按 userId 存十分钟。
这三件事凑在一起，足以让一张卡片把十个人**不同的、可能超过 24 小时**的区间加起来标成「24H」。

现在：记录带 `source / basis / windowStartTs / windowEndTs / fetchedTs`（获取时间与窗口右端分开）；
一次聚合只定一个窗口，十人共用；起点必须精确命中，否则判缺失；缓存键含口径与窗口；
聚合前校验成员完整性、数值有效性、口径一致性、窗口一致性，任一不满足即 `n/a` + 覆盖率 + 原因。
榜单实时值**不再**补进整点窗口的合计。详见 docs/FIELDS.md §3.1。

**② 全平台 24H 盈利人数**（新增 `top10PlatformProfitable24h`）

独立字段，用与合计**同一批记录**统计 `value > 0` 的人数，零收益不算盈利。
数据不完整或窗口不一致时，合计与人数一起显示不可用，绝不用该币的盈利人数补位。
另外新增 `top10SetStatus`，把「真的只有 3 个人」和「只采到 3 行」分开。

**③ 时间语义**（新增 `SourceTimes` / `StageResult`）

卡片上的偏移改由**持币采集阶段完成时刻**（`holdersDoneTs`）决定，不再是「整条流程跑完的时刻」。
以前 `attemptRecheck` 先取持币、再等 PnL、最后拿完成时间当持币偏移——PnL 慢一分钟，
卡片上的 `+5m1s` 就变成 `+6m1s`，而持币其实早就采完了。
链上区块时间 / 链上采集完成 / FOMO 响应 / 市值取值 / PnL 窗口起止 / PnL 获取完成分别保留；
两个来源时间差或数据年龄超限时卡片给降级说明。

**④ PnL 与持币关键路径解耦**

初值和持币复核都不再等 PnL：到期前 120 秒按到期时刻的整点窗口预取，到期时重新确定 Top10、
只复用成员与窗口都匹配的记录，没就绪就先出卡（`采集中…`），随后原地改写同一条卡片。
任务有状态（`alerts.pnl_state`）、截止时间、尝试上限和取消规则；撤回/失效的告警不会被迟到结果更新；
重启后未过期的接着补、过期的终结为 timeout。

调度侧同时做了两件事（**先补指标再改参数**）：
`HolderScheduler` 从「后台共用一个槽」改成**每种任务各占各的槽**（初值 2 / 复核 1 / 预热 1，全局 3），
一个已开跑的长预热不再能堵住复核——这正是上一轮那条 288 秒长尾的推断成因；
同时把排队等待与任务执行分开计量（`holder_queue_wait_ms` / `holder_run_ms` /
`nav_queue_wait_ms` / `nav_run_ms`），下次再出长尾就不用靠推断。
`platformPnl24h` 里每人固定等 5 秒的盲等换成**按 userId 等有效响应 + 超时**。

**2026-09-06 独立验收实跑的结果**（代码指纹 `c261bdf2…`，禁发送，生产规则，独立空库，
挂钟 60 分钟 / 有效观测 58 分钟，详见 docs/run/ACCEPTANCE.md §10）：

| 项 | 结果 |
|---|---|
| 持币复核相对原定到期 | P50 **3980 ms** / 最大 4582 ms（§9 开着 PnL 时是 **56606 ms**）|
| 卡片更新延迟（含补 PnL）| P50 4584 ms / 最大 8618 ms |
| PnL 单人取数 | P50 **1582 ms**（整改前是固定盲等 **5000 ms**）|
| 浏览器导航排队等待最大 | **1 ms**（PnL 不再堵住初值路径）|
| 初值延迟 | P50 **2317 ms** — **0.8 秒目标仍未达成，如实申报** |
| 触发 / 完整生命周期 / ERROR | 5 / 4 / **0** |
| Telegram 操作 | **0** |

长尾成因也从推断变成了观测：`prewarm` 单次执行实测能到 **38.2 秒**、排队 **13.3 秒**，
而本轮 4 次复核的排队等待**全部是 0**——旧的「复核与预热共用一个后台槽」确实会堵，
方向与 §5.3 的推断一致。但 38.2 秒**不足以**直接解释那条 288 秒，
所以那条仍算「方向被证实」，不算「已复现」。

**本轮抓到一个未解决问题**：跨过整点之后连续四批 PnL 取数一条记录都没产出
（41 次逐人取数全部 `ok`、平均 1.6 秒，`fomo_source` 全程 `ok`，无 401/403/430/431——
数据取回来了，是窗口校验把每条都判成缺失）。具体是缺起点还是缺终点，
本轮 `LOG_LEVEL=info` 看不到（原因写在 debug 日志里），**未定性，不猜**。
卡片显示 `采集中…（0/10）· 缺 10 人的收益记录`，任务终结为 `timeout`，
没有拿该币收益顶替、没有无限重试。见 ACCEPTANCE §10.9。

### 8.8 仍待确认（不要再声称已复刻）

1. 原版 Top10 是「全链地址前十」还是「FOMO 持币账户前十」——截图区分不了。本项目采用后者，是**暂定口径**。
2. 原版「全平台24H PnL」用实时口径还是整点口径，无从判定。
3. 原版「持币覆盖 49/336」的分子定义。
4. 原版三条 FOMO 阈值的真实值。两个样本只能给出「原版不严于此」的下界（≤13 / ≤7.8% / =0）。
5. 钱包身份的链上溯源器**尚未接入**，所以 `confirmed` 映射为 0。存储和接口已就位。
