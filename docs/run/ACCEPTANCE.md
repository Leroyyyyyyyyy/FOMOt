# 60 分钟实跑验收报告

> 数字部分在运行结束后由 `npm run report` 与 `scripts/capture-evidence.ts` 生成后填入，
> 本节先写清**怎么跑的**，免得事后拿含糊的口径解释数字。

## 1. 运行条件

| 项 | 值 |
|---|---|
| 通知模式 | `NOTIFY_MODE=off`（禁发送）。全程不做任何 Telegram 网络调用 |
| 规则 | `config/rules.yaml`（**生产规则**）。放宽规则的结果单独标注 |
| 代码指纹 | `bdbc1d73b4de5784`（`find src tests config -type f \( -name '*.ts' -o -name '*.yaml' \) \| sort \| xargs shasum \| shasum`）|
| 自动化测试 | 109 项全通过，`tsc --noEmit` 无错 |
| 数据库 | `data/fomot.db`；跑前用 `VACUUM INTO` 做过一致性备份 |
| 主机 | macOS (darwin 25.4.0)、Node v24.16.0 |

代码在开跑前**冻结**，运行期间没有改动 `src/` 或 `tests/`。
运行前的三次预跑（`dryrun-phase1/2/3.log`）都因为改了代码而作废重来，不计入验收。

## 2. 验收口径

- **禁发送**：`notification_log` 里 `mode='telegram'` 的行数必须为 **0**。
- **不把缺失显示为零**：卡片上任何 `n/a` 都必须伴随覆盖率，且不得由别的量顶替。
- **不把部分结果显示为完整**：Top10 收益只有在覆盖齐全、窗口一致时才给合计。
- **任务终态**：每条到期任务要么 `completed`，要么带可解释的 `abandoned`/`degraded` 与 `last_error`。
- **关键数字可复算**：`docs/run/evidence/samples.json` 里带够复算 Top10 收益与人数的原始行。

## 3. 指标口径

`run_metrics` 表按 `phase` 分 `startup`（进程启动后前 120 秒）与 `steady`。
两段分开统计——重启后 RPC 压力大、队列深，混在一起会同时高估稳态、低估恢复期。

| 指标 | 含义 |
|---|---|
| `scan_tick_ms` | 一次候选扫描（`engine.tick()`）耗时 |
| `initial_latency_ms` | **持币初值采集阶段完成**时刻 − 首次触发时刻。配置目标 800ms |
| `recheck_lateness_ms` | **持币复核采集阶段完成**时刻 − **原定**到期时刻。2026-09-06 起**不再包含** PnL 耗时 |
| `card_update_lateness_ms` | 卡片改写完成时刻 − 原定到期时刻。含后续补 PnL 的那一次改写 |
| `pnl_ready_ms` | 一次全平台收益取数的耗时（预取 / 补取分开记在 note 里）|
| `pnl_followup_lateness_ms` | PnL 补入卡片的时刻 − 原定到期时刻 |
| `pnl_batch_ms` / `pnl_user_ms` | 一批 / 单个用户的取数耗时（`ok` / `timeout` 记在 note 里）|
| `holder_queue_wait_ms` / `holder_run_ms` | 持币任务的**排队等待** / **执行耗时**，按 kind 分开 |
| `nav_queue_wait_ms` / `nav_run_ms` | 浏览器导航的排队等待 / 执行耗时，按 kind 分开 |
| `holder_queue_pending` / `holder_active` / `pnl_tasks_active` | 队列长度、并发数、在跑的 PnL 任务数 |
| `age_chain_ms` / `age_chain_taken_ms` / `age_fomo_ms` / `age_board_ms` | 各数据源的数据年龄，分别追溯 |
| `source_skew_ms` | 链上采集完成与 FOMO 响应之间的时间差 |
| `chain_lag_blocks` | 扫链游标落后链头多少块 |
| `recheck_attempts` / `recheck_retry` | 复核尝试次数 / 重试发生次数 |
| `unfinished_alerts` | 处于 `firing` 或 `pending_recheck` 的任务数 |
| `rss_mb` / `heap_mb` / `tracked_tokens` | 内存与持币缓存规模 |

> **2026-09-06 起口径变更**：`recheck_lateness_ms` 只量持币采集阶段，PnL 的耗时进
> `pnl_ready_ms` / `pnl_followup_lateness_ms`，整张卡片的更新延迟进 `card_update_lateness_ms`。
> 因此本轮的 `recheck_lateness_ms` **不能**与 §9 那轮（含 PnL）直接比大小，
> 要比就跟 §4 那轮（PnL 关闭）比。

---

# 4. 主验收结果（生产规则，禁发送，60 分钟）

日志：`dryrun-60min-primary.log`。窗口 2026-09-05 19:56 ~ 20:56 HKT。

## 4.1 底线检查

| 验收底线 | 结果 |
|---|---|
| 无 Telegram 网络操作 | ✅ `notification_log` 中 `mode='telegram'` 共 **0** 行；7 次 send + 8 次 edit 全是 `mode='off', ok=1` |
| 无未处理异常退出 | ✅ 日志 `ERROR` **0** 条、`unhandled` **0** 条，进程全程存活 |
| 不把缺失显示为零 | ✅ 全平台 24H PnL 一律 `n/a（0/10）`；榜单失效后显示 `n/a` 而非 0 人 |
| 不把部分结果显示为完整 | ✅ 10 条样本的 Top10 收益均为十人齐全才给合计 |
| 所有到期任务均完成或有可解释状态 | ✅ 窗口内 7 条全部 `completed` / `collection_state='complete'`，重试 0 次 |
| 关键数字可复算 | ✅ 见 §4.4 |
| 未确认身份不参与统计 | ✅ 钱包映射 908 条候选（437 迁移 + 471 新增），`confirmed` **0** |

## 4.2 指标（P50 / P95 / 最大）

启动恢复期 = 进程启动后 120 秒；其余为稳态。

| 指标 | 阶段 | n | P50 | P95 | 最大 |
|---|---|---|---|---|---|
| 候选扫描耗时 (ms) | 启动恢复 | 151 | 23 | 34 | 37 |
| 候选扫描耗时 (ms) | 稳态 | 3503 | 40 | 51 | 185 |
| 初值完成延迟 (ms) | 稳态 | 7 | **1686** | **6057** | **6057** |
| 复核相对**原定**到期的延迟 (ms) | 稳态 | 8 | 3921 | 6580 | 6580 |
| 链上数据年龄 (ms) | 稳态 | 7 | 2337 | 5496 | 5496 |
| FOMO 数据年龄 (ms) | 稳态 | 7 | 5532 | 25292 | 25292 |
| 榜单数据年龄 (ms) | 稳态 | 7 | 6356 | 47876 | 47876 |
| RPC 落后区块 | 稳态 | 59 | 0 | 0 | 0 |
| 复核尝试次数 | 稳态 | 8 | 0 | 0 | 0 |
| 未完成任务数 | 稳态 | 59 | 0 | 2 | 3 |
| 常驻内存 (MB) | 稳态 | 59 | 518 | 789 | 799 |
| 堆内存 (MB) | 稳态 | 59 | 148 | 410 | 505 |
| 持币缓存代币数 | 稳态 | 59 | 11 | 15 | 15 |

**RPC 错误与降级**：主端点→备用端点降级 **363** 次（约 6 次/分），每次都立刻回到主端点，
落后区块全程 0。这是既有行为（整改前约 17.5 次/分），本轮没有恶化，但**也没有解决**。

**复核重试 / 重复处理 / 未完成**：重试 0 次、重复处理 0 次、窗口结束时未完成 0 条。

**浏览器页面数**：常驻 1 个共享页；复核走临时页，用完即关。见 §4.5 的缺陷。

## 4.3 初值延迟没有达到 0.8 秒

配置目标 `snapshot_delay_ms: 800`，实测 P50 **1.69s** / P95 **6.06s**。

差距**不是**靠改标签掩盖的——卡片上印的 `(+1.7s)`、`(+6.1s)` 就是实测值本身。
差额来自 800ms 之后才发生的事：`blockClock.sync()` 一次 RPC、增量 `getLogs`、
以及 holder 调度器的排队。本轮已经去掉一处自己引入的回归
（快照曾为同一个区块多发两次 `getBlock`，改成复用 `sync()` 已取回的元数据后，
同一个币的初值从 +2.6s 降到 +1.8s）。**0.8 秒仍是未达成的性能目标，不作为已完成项申报。**

## 4.4 十条可复算样本

`docs/run/evidence/samples.json` 每条样本都带：链上区块号、`chainBlockTs` / `chainTakenTs` /
`fomoTakenTs` / `boardTakenTs` / `aggregatedTs` 五个分别的时间、Top10 的原始行
（`rank / user_id / handle / evm_address / amount / pnl`）、通知记录，以及 `recompute` 字段
（Top10 该币收益合计、盈利人数、识别数），任何人都能用原始行重算卡片上的数字。

| 代币 | 初值 | 复核 | Top10 该币累计收益 | 盈利 | 识别 | 全平台24H |
|---|---|---|---|---|---|---|
| ANT | +1669ms 全链 251 / Fomo 12 | +305144ms 全链 104 / Fomo 17 | −379.76 | 0 | 10/10 | n/a |
| BILLY | +4227ms 全链 381 / Fomo 91 | +307580ms 全链 663 / Fomo 275 | +13258.70 | 10 | 10/10 | n/a |
| NSDX | +1686ms 全链 197 / Fomo 10 | +303789ms 全链 193 / Fomo 15 | −171.59 | 0 | 10/10 | n/a |
| ORCA | +1398ms 全链 468 / Fomo 104 | +303937ms 全链 535 / Fomo 158 | −223.54 | 2 | 10/10 | n/a |
| FRIEND | +6057ms 全链 210 / Fomo 36 | +306860ms 全链 280 / Fomo 119 | −996.78 | 3 | 10/10 | n/a |
| DINGO | +1649ms 全链 125 / Fomo 9 | +303261ms 全链 136 / Fomo 11 | +670.75 | 8 | 10/10 | n/a |
| COLLECT | +1823ms 全链 473 / Fomo 89 | +304561ms 全链 447 / Fomo 94 | −740.54 | 0 | 10/10 | n/a |

注意 ANT 的全链持币从 251 掉到 104——这是真实的抛售，不是采集错误；
两次快照的区块号（55121759 → 55124734）都在样本里，可以独立核对。

## 4.5 实跑抓到的缺陷（已修，但**不在**主验收窗口内验证）

**现象**：12:33 起 `refreshLeaderboard` 每 5 分钟失败一次，
`page.goto: Target page, context or browser has been closed`，共 6 次。
共享页被关掉后没有任何地方重建它，`tokenStats` 也再没法导航，
于是所有候选币永远停在「待预热」，**12:33 之后再没有产生过任何触发**
（7 次触发全部集中在前 33 分钟）。而 `ready` 始终是 true，
`recoverAuth()` 只在 `!ready` 时才跑，所以系统不会自愈。

**没有说假话**：榜单数据龄超过 10 分钟后 `leaderboardAvailable` 转 false，
卡片显示 `n/a`、健康行转红。第四阶段要的「降级而不是拿旧数据冒充」是成立的。
但可用性上这是个硬伤：数据源一挂，告警就整体停摆。

**修复**（`src/fomo/scraper.ts`）：
1. 新增 `ensurePage()`，`refreshLeaderboard` / `tokenStats` / `recoverAuth` 三条路径导航前都先确保页面可用；
2. 新增 `markStaleIfNeeded()`：关键业务接口超过 10 分钟没成功就主动把 `ready` 置 false、
   健康位记 `stale`，让 Engine 走 `!fomo.ready` 分支降级为纯链上模式继续告警；
3. 后台循环不再只看 `ready`，每轮先判 staleness 再决定是否 `recoverAuth()`。

回归测试见 `tests/fomo-health.test.ts`（5 项）。

**这个修复是在主验收窗口结束之后做的**，所以 §4.1/§4.2 的数字反映的是**修复前**的代码。
修复后的验证跑结果单列在 §5，不与主验收混算。

---

# 5. 修复验证跑（单列，不与主验收混算）

日志：`dryrun-verify.log`。窗口 2026-09-05 21:00 ~ 21:47 HKT，共 **45 分钟**，生产规则，禁发送。
代码 = 主验收代码 + §4.5 的页面自愈修复 + `tests/fomo-health.test.ts`（共 114 项测试通过）。

## 5.1 缺陷是否修好

| 判据 | 主验收（修复前）| 验证跑（修复后）|
|---|---|---|
| 榜单刷新成功次数 | 7（全在前 33 分钟）| **9** |
| 「刷新盈利榜失败」 | **6**（+36.5 分钟起每 5 分钟一次）| **0** |
| `Target page … has been closed` | 6 | **0** |
| 触发次数 | 7（**+33 分钟后归零**）| **16**（+34.6 分钟仍在触发）|
| 越过 +36.5 分钟这个翻车点 | ❌ | ✅ 在 +34 / +39 / +44 分钟均正常刷新 |

结论：**已修复并验证**。数据源在窗口结束时仍实时健康
（`fomo_business_ok_ts` 0 秒前、`待预热` 1 个、`fomo_source=ok`）。

## 5.2 验证跑指标

| 指标 | 阶段 | n | P50 | P95 | 最大 |
|---|---|---|---|---|---|
| 候选扫描耗时 (ms) | 启动恢复 | 120 | 54 | 146 | 193 |
| 候选扫描耗时 (ms) | 稳态 | 2580 | 56 | 96 | 793 |
| 初值完成延迟 (ms) | 稳态 | 16 | 2373 | 6431 | 6431 |
| 复核相对**原定**到期的延迟 (ms) | 稳态 | 8 | 5269 | 287613 | **287613** |
| 链上数据年龄 (ms) | 稳态 | 16 | 3103 | 6834 | 6834 |
| FOMO 数据年龄 (ms) | 稳态 | 16 | 3648 | 47764 | 47764 |
| 榜单数据年龄 (ms) | 稳态 | 16 | 3764 | 19158 | 19158 |
| RPC 落后区块 | 稳态 | 43 | 0 | 0 | 0 |
| 复核尝试次数 | 稳态 | 8 | 0 | 0 | 0 |
| 未完成任务数 | 稳态 | 43 | 1 | 4 | 4 |
| 常驻内存 (MB) | 稳态 | 43 | **857** | 903 | **906** |
| 堆内存 (MB) | 稳态 | 43 | 340 | 451 | 557 |
| 持币缓存代币数 | 稳态 | 43 | 9 | 13 | 13 |

窗口内 8 条告警全部 `completed` / `complete`，重试 **0** 次；
通知 8 次 send + 8 次 edit，**全部 `mode='off'`**。

## 5.3 两个未解决的问题（实测量出来的，不是猜的）

### 复核延迟有长尾：一条晚了 288 秒

`0x88942e2016…`（RSO）21:08:24 触发、原定 21:13:25 到期、实际 21:18:12 完成，
**晚 288 秒，且重试次数为 0**。不是重启造成的（整条生命周期都在验证跑窗口内），
也没有数据丢失——最终状态是 `completed` / `complete`。

8 条复核里 P50 只有 5.3 秒，所以这是长尾而非普遍现象。
**最可能的成因**是持币调度器只给后台任务一个槽：`recheck` 在**排队顺序**上优先于 `prewarm`，
但一旦某个 `prewarm` 已经开跑（老币要从部署区块全量扫，可能好几分钟），
`recheck` 就得等它跑完。**这只是推断，日志不足以证实**——要坐实需要给调度器加
「排队等待时长」和「任务耗时」两个指标。在那之前不宜直接改并发参数。

按验收底线这条是**通过**的（到期任务完成了，状态可解释），但它是 0.8 秒性能目标之外
另一个真实的时延问题，记在这里而不是掩盖。

### 内存偏高且在长跑中增长

主验收稳态 RSS P50 518MB / 最大 799MB；验证跑 P50 **857MB** / 最大 **906MB**。
持币缓存只有 9~13 个代币，`sweepHolders` 在正常工作，所以主要占用应该在 Playwright 侧。
连跑几周的目标下这需要盯，本轮**未解决**。

## 6. 样本与证据

> 这一节说的是 2026-09-05 那一轮的样本文件，现已归档为
> `docs/run/evidence/samples-20260905.json`（`samples.json` 现在放的是 §10 本轮的样本）。

`samples-20260905.json` 共 **13 条**样本（主验收 10 + 验证跑 3），每条都带：

- 链上区块号与区块时间、采集完成时间；
- `chainTakenTs` / `fomoTakenTs` / `boardTakenTs` / `aggregatedTs` 四个**分别**的来源时间；
- Top10 的原始行（`rank / user_id / handle / evm_address / amount / pnl`）；
- `recompute`：Top10 该币累计收益合计、盈利人数、识别数——可独立复算卡片上的数字；
- 该告警的全部通知记录（`mode` / `op` / `ok` / 卡片正文）。

累计 **12 条**告警完成了初值 + 复核（要求 ≥10）。

## 7. 失败案例清单

| 案例 | 处理 |
|---|---|
| 共享浏览器页被关闭 → 榜单刷新与预热全线失败、触发停摆 | §4.5，已修复并在 §5.1 验证 |
| 一条复核晚 288 秒 | §5.3，未解决，已记录并给出待证实的成因 |
| 主端点 RPC 降级 363 次/小时 | 既有行为，未恶化也未解决 |
| 内存 45 分钟内涨到 906MB | §5.3，未解决 |
| 榜单失效期间卡片显示 | **正确降级**：`leaderboardAvailable=false` → 显示 `n/a`、健康行转红，未拿旧数据冒充 |

## 8. 与验收底线逐条对照

| 底线 | 结果 |
|---|---|
| 不把缺失数据显示为零 | ✅ |
| 不把部分结果显示为完整 | ✅ |
| 无未处理异常退出 | ✅ 两轮共 105 分钟，`ERROR` 0、`unhandled` 0 |
| 无已确认的数据污染和重复计数 | ✅ Top10 按 userId 去重；`swaps` 主键保证重放幂等 |
| 所有到期任务均完成或有可解释的降级/重试状态 | ✅ 20 条全部 `completed`/`complete` |
| 关键数字能用对应数据源复算 | ✅ 13 条样本带原始行与 `recompute` |
| 无 Telegram 网络操作 | ✅ `notification_log` 全库统计：`telegram` **0** 条，`off` **36** 条 |
| 0.8 秒不靠改标签达成 | ✅ **未达成，如实申报**：初值 P50 1.69s / P95 6.06s（主验收） |

---

# 9. 全平台 24H PnL 默认开启后的 60 分钟跑

日志：`dryrun-default-on.log`。窗口 2026-09-05 22:37:51 ~ 23:37:51 HKT，生产规则，禁发送，
**进程启动时不带任何 `FOMO_PLATFORM_PNL` 环境变量**——走的就是新的默认值。

## 9.1 为什么改成默认开启，以及改之前踩的坑

`platformPnl24h` 原本在 `enrich()` 里无条件调用，而 `enrich()` 在**初值**路径上。
第一次实跑就暴露了代价：

| 取数位置 | 初值延迟（实测）|
|---|---|
| 初值路径（原实现）| **70,966 ms / 49,127 ms** |
| 只在复核（现行）| **3,210 ms** |

Top10 十个人各要逛一次档案页，约 71 秒——`+0.8s` 的预算根本放不下。

改法有原图背书：原版截图那行标的是「Top10持币账户（**+5m1s**）」，
`+5m1s` 是复核阶段的偏移，说明**原版同样在复核阶段算这个数**。
现行实现加了 `priority === 'recheck'` 条件，初值卡先显示 `n/a`，五分钟后原地改写补上。
回归测试：`tests/recheck.test.ts`「全平台 24H 收益只在复核阶段取」。

## 9.2 结果

| 项 | 值 |
|---|---|
| 触发 | 37 |
| 完成完整生命周期 | 15（全部 `completed` / `complete`）|
| `ERROR` / 未捕获异常 | **0 / 0** |
| 复核重试 | **0** |
| 榜单刷新 / 失败 | 11 / **0**（页面自愈修复持续有效）|
| 通知 | 16 send + 15 edit，**全部 `mode='off'`；Telegram 操作 0** |

## 9.3 指标（与关闭时对比）

| 指标 | 阶段 | P50 | P95 | 最大 | 关闭时 P50（主验收）|
|---|---|---|---|---|---|
| 候选扫描耗时 (ms) | 稳态 | 60 | 105 | 3313 | 40 |
| **初值完成延迟 (ms)** | 稳态 | **2121** | 4799 | 5189 | **1686**（同档，未被拖累）|
| **复核相对原定到期的延迟 (ms)** | 稳态 | **56606** | 82742 | 82742 | **3921** |
| 链上数据年龄 (ms) | 稳态 | 2817 | 5853 | 6299 | 2337 |
| FOMO 数据年龄 (ms) | 稳态 | 3439 | 37063 | 41851 | 5532 |
| RPC 落后区块 | 稳态 | 0 | 0 | 0 | 0 |
| 复核尝试次数 | 稳态 | 0 | 0 | 0 | 0 |
| 常驻内存 (MB) | 稳态 | 702 | 779 | 783 | 518 |
| 持币缓存代币数 | 稳态 | 13 | 24 | 25 | 11 |

**代价是明确的**：复核延迟 P50 从 3.9 秒变成 **56.6 秒**（卡片上从 `+5m1s` 变成约 `+5m56s`），
就是那十次档案页导航。初值不受影响。
上一轮那个 288 秒的复核长尾**没有复现**，本轮最大 82.7 秒。

## 9.4 填充率与四个 `n/a` 的成因

15 张复核卡片里 **11 张拿到真实数值**（含 3 个负数：−$6.67K / −$26.43K / −$52.36），4 张 `n/a`：

| 币 | 账户识别 | `n/a` 成因 |
|---|---|---|
| PILL / NOTHING / BODKIN | **0/0** | 复核时该币 FOMO 侧一个持币人都没有，Top10 是空集；「该币累计收益」同样是 `n/a（0/0）`，两个字段一致 |
| LYMBIC | 10/10 | 十人只取到 **9** 个序列 → `n/a（9/10）`。**设计意图**：差一个就不给合计 |

LYMBIC 最能说明两个字段是**各自独立**判断覆盖率的：
该币累计收益 `+$3.34K · 盈利 10 人`（齐全，照给），全平台 `n/a（9/10）`（不齐，不给）。

## 9.5 开启后的运维注意

1. 对 fomo.family 的请求量涨约 10 倍，长期跑更容易撞机器人检测（`430`）；
2. 内存稳态 702MB，与关闭时同档，没有因为多开临时页而恶化；
3. `FOMO_PLATFORM_PNL=0` 可关，关掉该字段回到 `n/a`，其余行为不变。


---

# 10. 2026-09-06 整改后的独立验收（窗口校验 / 独立盈利人数 / 时间语义 / PnL 解耦）

**这一轮是独立的，不沿用 §4、§5、§9 的任何结论。**代码变了，指标口径也变了（见 §3 的口径变更说明）。

## 10.1 运行条件

| 项 | 值 |
|---|---|
| 通知模式 | `NOTIFY_MODE=off`（禁发送）。全程不做任何 Telegram 网络调用 |
| 规则 | `config/rules.yaml`（**生产规则**），未改任何阈值 |
| 开关 | `FOMO_PLATFORM_PNL` **未设置**（走默认：开启）；其余 PnL/时间阈值均为默认值 |
| 代码指纹 | `c261bdf2a6900201212fd61d12d47f430f5e32ed`（`find src tests config -type f \( -name '*.ts' -o -name '*.yaml' \) \| sort \| xargs shasum \| shasum`）|
| 自动化测试 | **164 项全通过**，`tsc --noEmit` 无错 |
| 数据库 | **独立库** `data/acceptance/run.db`（全新空库）。**没有**读写、也没有重置日常运行的 `data/fomot.db` |
| 日志 | `docs/run/logs/dryrun-v2.log` |
| 主机 | macOS (darwin 25.4.0)、Node v24.16.0 |

代码在开跑前**冻结**（指纹如上），运行期间没有改动 `src/`、`tests/`、`config/`。
本轮之前有四次预跑因为开跑后又改了代码而**作废**（含把 PnL 取数移出共享导航队列、
把缓存锚点选择抽成可单测的纯函数两处改动），
一律不计入验收，也没有引用它们的任何数字；每次作废都重建了空库重跑。

> 用独立空库跑的代价要说清楚：冷启动要重放约一小时的链上区块才能把成交量窗口填满
> （`volume_window_complete`），这段时间不产生任何触发，所以有效观测窗口小于挂钟时长。
> 换来的是「本轮数字只来自本轮」——不会有旧库里的历史行混进统计。

## 10.2 默认阈值

| 常量 | 默认值 | 作用 |
|---|---|---|
| `FOMO_PNL_PREFETCH_LEAD_MS` | 120s | 复核到期前多久开始预取 |
| `FOMO_PNL_FOLLOWUP_DEADLINE_MS` | 600s | PnL 补取期限（相对原定到期），超时终结为 `timeout` |
| `PNL_FOLLOWUP_MAX_ATTEMPTS` / `PNL_FOLLOWUP_RETRY_MS` | 2 次 / 45s | 补取尝试上限与间隔 |
| `FOMO_PLATFORM_PNL_WAIT_MS` | 12s | 等**单个用户**的 aggregatedSnapshot 响应上限（取代原来的固定 5s 盲等）|
| `FOMO_PLATFORM_PNL_BUDGET_MS` | 150s | 一批 Top10 取数的总预算 |
| `FOMO_MAX_SOURCE_SKEW_MS` / `FOMO_MAX_SOURCE_AGE_MS` | 120s / 600s | 两来源时间差 / 单来源数据年龄上限，超限卡片降级 |
| `HolderScheduler` 槽位 | 全局 3；初值 2 / 复核 1 / 预热 1 | 明确的并发上限 |
| FOMO 导航队列 | 单页串行，优先级 复核 > 告警 > 预热 > 后台；带排队截止时间 | 明确的队列上限 |
| PnL 页面 | 一次取数开 **1 个**临时页，用完即关 | 明确的页面数上限 |

## 10.3 强制退出恢复演练（隔离环境，独立临时库）

`npx tsx scripts/crash-recovery-drill.ts`，子进程写完状态后 `SIGKILL`（退出码 137），新进程恢复：

```
first : {"resumed":1,"orphanFiring":2,"corrupt":1,"pnlResumed":0}
second: {"resumed":0,"orphanFiring":0,"corrupt":0,"pnlResumed":0}   ← 重复恢复是空操作
  trigger_ts=1000 status=pending_recheck  采集=pending
  trigger_ts=2000 status=abandoned 采集=degraded  err=异常退出遗留 firing；已发出 message_id=…，缺少复核状态
  trigger_ts=4000 status=abandoned 采集=degraded  err=恢复 payload 失败: SyntaxError…
telegram 模式的通知记录数: 0
```

值得单说的两点：

1. `trigger_ts=1000` 那条用的是**整改前的扁平 payload**（`initE` / `initialOffsetMs`），
   新代码通过 `normalizeStored()` 照样恢复，且偏移按 `triggerTs + 原偏移` 还原——不是凭空造一个。
2. 恢复流程**没有产生任何 `send`**：已发出但状态未落库的那条被标成 `abandoned/degraded` 等人看，
   从没发出去的占位行被删掉。重启不会重发初值卡片。


## 10.4 主结果（生产规则，禁发送）

窗口 **2026-09-06 13:08:39 ~ 14:09:00 HKT**（挂钟 60 分钟）。
其中 13:08:39~13:11:04 是冷启动重放一小时链上区块的预热期，**不产生触发**，
所以有效观测窗口 **约 58 分钟**。日志 `docs/run/logs/dryrun-v2.log`。

| 项 | 值 |
|---|---|
| 触发 | **5**（1 条被快照门拒掉，未推送）|
| 完成完整生命周期 | **4**，全部 `completed` / `collection_state='complete'` |
| 复核撤回 | 0 |
| 复核重试 | **0** |
| `ERROR` / 未捕获异常 | **0 / 0** |
| 通知 | 4 次 send + 6 次 edit，**全部 `mode='off'`；Telegram 操作 0** |
| PnL 任务终态 | `ready` 2 / `timeout` 2；过期仍 `pending` **0** |

### 底线检查

| 验收底线 | 结果 |
|---|---|
| 无 Telegram 网络操作 | ✅ `notification_log` 中 `mode='telegram'` **0** 行 |
| 无未处理异常退出 | ✅ `ERROR` 0、`unhandled` 0，进程全程存活 |
| 不把缺失显示为零 | ✅ 4 张卡的全平台行要么是真实数值，要么是 `采集中…（N/M）· 原因`，没有一次显示 0 |
| 不把部分结果显示为完整 | ✅ 预取 8/10、9/10 时卡片显示 `采集中…`，补齐到 10/10 才给合计 |
| 所有到期任务均完成或有可解释状态 | ✅ 4 条全 `completed/complete`；2 条 PnL 明确终结为 `timeout` |
| 关键数字可复算 | ✅ `docs/run/evidence/samples.json` 带逐人原始行与各阶段时间 |

## 10.5 任务四：持币偏移不再被 PnL 撑大（直接观测）

| 代币 | 初值偏移 | 持币复核偏移 | 最后一次卡片改写 | 差值 = PnL 等待 |
|---|---|---|---|---|
| 🅿️ | +3.1s | **+5m5s** | +5m10s | 4.6s |
| p402 | +1.6s | **+5m5s** | +5m9s | 4.8s |
| ROBIN | +1.6s | **+5m6s** | +5m6s | —（PnL 未就绪）|
| asd | +2.3s | **+5m4s** | +5m4s | —（PnL 未就绪）|

🅿️ 那条最能说明问题——**同一张卡片先后两个版本**（完整正文见 `samples.json`）：

```
edit +304982ms   持币复核（+5m5s）: 全链 4512 · Fomo 1830 (40.6%)
                 该币累计收益（已实现+未实现）: -$22.30K · 盈利 5 人
                 全平台24H PnL: 采集中…（7/10）· 缺 3 人的收益记录
edit +309618ms   持币复核（+5m5s）: 全链 4512 · Fomo 1830 (40.6%)   ← 偏移、人数一字未改
                 该币累计收益（已实现+未实现）: -$22.30K · 盈利 5 人
                 全平台24H PnL: +$636.42K · 盈利 8 人 · 整点对齐口径（截至 09/06 13:00）
```

PnL 晚了 4.6 秒，`+5m5s` 没有变成 `+5m10s`——这正是整改前会发生的事。

## 10.6 任务一 / 二：两行收益各自独立（本轮真实卡片）

| 代币 | 该币累计收益（全时段）| 全平台 24H（整点对齐）|
|---|---|---|
| 🅿️ | **−$22.30K · 盈利 5 人** | **+$636.42K · 盈利 8 人** |
| p402 | **+$35.79K · 盈利 10 人** | **−$20.39K · 盈利 7 人** |
| ROBIN | −$221.71 · 盈利 0 人 | `采集中…（0/5）· 缺 5 人的收益记录` |
| asd | +$2.68K · 盈利 4 人 | `采集中…（0/10）· 缺 10 人的收益记录` |

前两条同时出现了**两个方向**的反号：🅿️ 该币为负、全平台为正；p402 该币为正、全平台为负。
盈利人数也各不相同（5 vs 8、10 vs 7）。整改前这两行会是同一个数字配同一个人数——
这就是把该币累计收益标成「全平台24H PnL」的具体后果。

ROBIN 那条的分母是 **5** 而不是 10：该币 FOMO 侧实际只有 5 个持币账户，
按实际成员集合统计并显示真实分母，没有补齐到十。

## 10.7 任务三：分阶段耗时（稳态 P50 / P95 / 最大）

| 指标 | n | P50 | P95 | 最大 |
|---|---|---|---|---|
| 初值完成延迟 (ms) | 5 | **2317** | 3104 | 3104 |
| **持币复核相对原定到期 (ms)** | 4 | **3980** | 4582 | **4582** |
| 卡片更新延迟 (ms) | 6 | 4584 | 8618 | 8618 |
| PnL 补入相对原定到期 (ms) | 2 | 8618 | 8618 | 8618 |
| PnL 单批取数 (ms) | 10 | 15104 | 26597 | 26597 |
| **PnL 单人取数 (ms)** | 67 | **1582** | 2281 | 12002（1 次超时）|
| 持币任务排队等待 (ms) | 15 | 0 | 13337 | 13337 |
| 持币任务执行 (ms) | 15 | 1705 | 38203 | 38203 |
| 浏览器导航排队等待 (ms) | 68 | 0 | 0 | **1** |
| 浏览器导航执行 (ms) | 68 | 1244 | 1539 | 1847 |
| 常驻内存 (MB) | 58 | 521 | — | 624 |
| 持币队列长度 / 并发 / PnL 任务数 | 58 | 0 / 0 / 0 | — | 1 / 1 / 1 |

### 复核延迟：与前两轮的对照

`recheck_lateness_ms` 的口径本轮变了（只量持币采集阶段），所以只能跟同口径的比：

| 轮次 | PnL 开关 | 复核延迟 P50 | 最大 |
|---|---|---|---|
| §4 主验收 | 关 | 3921 ms | 6580 ms |
| §5 验证跑 | 关 | 5269 ms | **287613 ms**（长尾）|
| §9 默认开启 | 开（复核内同步取）| **56606 ms** | 82742 ms |
| **§10 本轮** | **开（已解耦）** | **3980 ms** | **4582 ms** |

也就是说：**开着 PnL，复核延迟回到了 PnL 关闭时的水平**（3980 vs 3921 ms），
而 §9 那轮开着 PnL 时是 56.6 秒。这是本轮最主要的性能结果。

### 长尾成因：从推断变成观测

§5.3 那条 288 秒的复核长尾，当时的成因是**推断**（「复核可能被已开跑的 prewarm 堵住」），
并明确写了「日志不足以证实」。本轮先补了分阶段指标，现在有数：

| 任务类型 | 排队等待最大 | 执行耗时最大 |
|---|---|---|
| `initial` | **0 ms** | 2269 ms |
| `recheck` | **0 ms** | 3164 ms |
| `prewarm` | **13337 ms** | **38203 ms** |

`prewarm` 单次执行确实能跑到 **38.2 秒**、排队能等 **13.3 秒**——
在旧的「复核与预热共用一个后台槽」策略下，一个已经开跑的预热就会把复核堵住这么久，
与当初的推断方向一致。改成每类任务各占各的槽之后，**本轮 4 次复核的排队等待全部是 0**。

> 说明边界：本轮观测到的 prewarm 最长执行是 38.2 秒，**不足以直接解释 288 秒**。
> 那一条的成因仍然只是「方向被证实」，不是「已完全复现」。老币从部署区块全量扫
> 需要多久，取决于该币的历史长度，本轮样本里没有那么老的币。

### PnL 取数：盲等换成等有效响应

| | 整改前 | 本轮实测 |
|---|---|---|
| 每人等待 | 固定 **5000 ms** | P50 **1582 ms** / P95 2281 ms |
| 超时 | 无超时概念 | 12s 上限，67 次里 **1 次**触发 |
| 是否占共享导航队列 | 是 | **否**（独立单槽锁）|
| 导航排队等待最大 | — | **1 ms** |

导航排队等待最大只有 1 毫秒，说明 PnL 那批最长 26.6 秒的取数**没有**再堵住初值路径的页面导航。

## 10.8 初值延迟：0.8 秒仍未达成，如实申报

配置目标 `snapshot_delay_ms: 800`，本轮实测 P50 **2317 ms** / 最大 **3104 ms**。

- 与 §4（1686 ms）、§9（2121 ms）同一量级，本轮**没有改善也没有恶化**；
- 卡片上印的 `(+3.1s)`、`(+1.6s)` 就是实测值本身，**不是**靠改标签达成的；
- 差额来自 800 ms 之后才发生的事：`blockClock.sync()` 一次 RPC、增量 `getLogs`、
  以及持币调度器的排队（本轮初值排队等待实测为 **0**，所以排队不是本轮的瓶颈，
  瓶颈在 RPC 与日志扫描本身，见 `holder_run_ms` initial 最大 2269 ms）。

**结论：0.8 秒是未达成的性能目标，不作为已完成项申报。**
截图上的 `0.6~0.8 秒` 作为对照目标保留在这里，本轮差距 1.5~2.3 秒。

## 10.9 本轮抓到的未解决问题

### 跨过整点后，连续四批 PnL 取数一条记录都没产出

**事实（不是推断）**：

| 时刻 | 批次 | 结果 |
|---|---|---|
| 13:16 / 13:18 | prefetch → followup | 8/10 → **10/10** ✅ 窗口 `截至 13:00` |
| 13:47 / 13:49 | prefetch → followup | 9/10 → **10/10** ✅ 窗口 `截至 13:00` |
| 14:04 ~ 14:08 | prefetch ×2 + followup ×4 | **0/2、1/10、0/10、0/5、0/10、0/5** ❌ |

失败时段里 **41 次逐人取数全部 `ok`**（平均 1.6 秒，与成功时段一致），
`fomo_source` 到窗口结束都是 `ok`，日志里没有 401/403/430/431。
所以**不是**取数失败、不是鉴权失败、不是机器人检测——数据取回来了，是**窗口校验把每一条都判成了缺失**。

**没有证明的部分**：具体每个人是 `no_end_point` 还是 `no_start_point`，本轮**看不到**——
这个原因写在 `log.debug` 里，而本轮跑的是 `LOG_LEVEL=info`。
时间上它与「跨进 14:00 这个整点」高度吻合，但**我不把吻合当成因果**。

**下一步（明确可执行）**：用 `LOG_LEVEL=debug` 复跑，代码已经会打印
`全平台收益部分缺失 { 缺失: [userId:reason, …] }`，一轮就能定位到底是哪一端的点缺失，
再决定是放宽 `MAX_ANCHOR_LAG_MS`（目前恰好 1 小时）还是改锚点选择策略。

**没有说假话**：这四条的卡片显示的是 `采集中…（0/10）· 缺 10 人的收益记录`，
两条告警的 PnL 任务在期限内重试 2 次后终结为 `timeout`，卡片保持 `n/a`。
**没有拿该币收益顶替，没有无限重试，没有把 0 当成收益。**
这正是严格窗口校验应有的行为：宁可这一格空着，也不印一个证明不了的数字。

### 样本量偏小

有效窗口 58 分钟内只有 5 次触发、4 条完整生命周期，少于 §4（7 条）和 §9（15 条）。
生产规则下的触发密度本来就随行情波动，本轮**没有**为了凑样本去放宽规则。
P50/P95 在 n=4~5 上意义有限，**不应**当成稳定的性能结论；
`pnl_user_ms`（n=67）与 `nav_queue_wait_ms`（n=68）样本量足够，那两项的结论更可靠。

### 既有问题：未复现也未解决

- **RPC 主端点降级**：本轮仍在发生，落后区块全程 0。属既有行为，本轮未处理。
- **内存**：稳态 RSS P50 521 MB / 最大 624 MB，比 §5 的 857/906 MB 低，
  但样本只有 58 分钟，**不足以**说明长跑内存问题已解决。
